class OpCheck{
	method void main(){
	var int i,base,offset;
	var boolean t;
	let i = 0;
	let base = 2;
	let offset = 10;
	let t = base & i;
	let t = offset | base;
	let t = base < offset ; 
	let t = base > offset ; 
	let t = base = offset ; 
	return;
	}
}
